language: c++

sudo: false

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
      - llvm-toolchain-precise-3.6
    packages:
      - llvm-3.6
      - llvm-3.6-dev
      - clang-3.6
      - libedit-dev

script:
  - make
  - make test
